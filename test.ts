// arrow-parens
export const identity: <T>(v: T) => T = v => v

// interface-name
export interface Shape {
	bar: string
}

// indent
// trailing-comma:multiline
// quotemark
export const a = {
	b: 'c',
}

// trailing-comma:singleline
export const b = { c: 42 }
